const themeName = 'appTheme';
const primaryColor = '#f22735';

export const theme = {
  defaultTheme: themeName,
  themes: {
    appTheme: {
      dark: false,
      colors: {
        primary: primaryColor,
      },
    },
  },
};
